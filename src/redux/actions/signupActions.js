import * as types from '../types/types';

export const signup = (params) => {        
    return  {
       type: types.SIGNUP_API,
       params
      }
  }                                                                                                                                                                                                                                               