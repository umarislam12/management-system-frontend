import * as types from '../types/types';

export const login = (params) => {        
    return  {
       type: types.LOGIIN_API,
       params
      }
  }                                                                                                                                                                                                                                               